import { Request, Response } from "express";
import { CourseWorkModel } from "@fcai-sis/shared-models";

type HandlerRequest = Request;

/*
 * Creates course work
 * */
const createCourseWorkHandler = async (
  req: HandlerRequest,
  res: Response
) => {};

export default createCourseWorkHandler;
