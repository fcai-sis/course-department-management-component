import { Router } from "express";

const courseRoutes = (router: Router) => {
  /*
   * Create a new course
   * */
  // router.post("/create", asyncHandler(createCourseWorkHandler));
};

export default courseRoutes;
